import style from './AddChannel.module.scss'
const AddChannelBtn = () => {
	return (
		<button className={style.wrapperBtnChannel}>
			Добавить свой канал на биржу
		</button>
	)
}

export default AddChannelBtn
